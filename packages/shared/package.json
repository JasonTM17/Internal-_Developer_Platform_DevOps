{
  "name": "@idp/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript interfaces, types, and utilities",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "lint": "eslint . --ext .ts,.tsx",
    "lint:fix": "eslint . --ext .ts,.tsx --fix",
    "typecheck": "tsc --noEmit",
    "test": "echo 'no tests yet'",
    "test:unit": "echo 'no tests yet'",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
