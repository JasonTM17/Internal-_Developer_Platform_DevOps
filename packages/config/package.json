{
  "name": "@idp/config",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "description": "Shared configuration schemas, validation, and defaults for the Internal Developer Platform",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "lint": "eslint . --ext .ts,.tsx",
    "lint:fix": "eslint . --ext .ts,.tsx --fix",
    "typecheck": "tsc --noEmit",
    "test": "echo 'no tests yet'",
    "test:unit": "echo 'no tests yet'",
    "clean": "rimraf dist"
  },
  "files": [
    "eslint",
    "prettier",
    "typescript",
    "src"
  ],
  "dependencies": {
    "@idp/shared": "workspace:*",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
