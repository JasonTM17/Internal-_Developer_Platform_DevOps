{
  "name": "@idp/ui",
  "version": "0.1.0",
  "private": true,
  "description": "Shared UI components for the portal",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "lint": "eslint . --ext .ts,.tsx",
    "lint:fix": "eslint . --ext .ts,.tsx --fix",
    "typecheck": "tsc --noEmit",
    "test": "echo 'no tests yet'",
    "test:unit": "echo 'no tests yet'",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@idp/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
