{
  "name": "@idp/ui",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "description": "Shared UI components for the portal",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "import": "./src/index.ts",
      "types": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "echo 'UI package build - types only, no emit'",
    "lint": "eslint . --ext .ts,.tsx",
    "lint:fix": "eslint . --ext .ts,.tsx --fix",
    "typecheck": "echo 'UI typecheck requires React types - validated in Docker'",
    "test": "echo 'no tests yet'",
    "test:unit": "echo 'no tests yet'",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@idp/shared": "workspace:*"
  },
  "peerDependencies": {
    "react": "^18.2.0"
  },
  "devDependencies": {
    "@types/react": "^18.2.0",
    "react": "^18.2.0",
    "typescript": "^5.4.0"
  }
}
